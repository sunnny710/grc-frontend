{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist/out-tsc",
    "types": [
      "jasmine"
    ],
    "rootDir": "src"  // <--- เพิ่มบรรทัดนี้เข้าไปเพื่อให้มองจากจุดเริ่มต้นเดียวกัน
  },
  "include": [
    "src/**/*.spec.ts",
    "src/**/*.d.ts"
  ]
}